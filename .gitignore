*.iml
.gradle
/local.properties
/.idea
/.vscode
/DroidVM-Prebuilt-Root
/DroidVM-Prebuilt-Root_
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Legacy native-util source locations -- the daemons + lbx are now built by the
# DroidVM-Prebuilt-Root pipeline, not staged into the app tree.
/app/src/thirdparty
/app/src/main/rust

# Stale daemon binaries from the old build.sh staging (now delivered via the
# prebuilt tar.xz); ignore leftovers so they are never packaged or committed.
/app/src/main/assets/bin/*/bridgedhcp
/app/src/main/assets/bin/*/gvswitch
/app/src/main/assets/bin/*/lbx
/app/src/main/assets/bin/*/netbox
/app/src/main/assets/bin/*/pbridge

# Terminal font: build.sh fetches the ~2.3MB Maple Mono NL NF TTF on first
# build; only the OFL license text under app/src/main/assets/fonts is committed.
/app/src/main/assets/fonts/*.ttf
